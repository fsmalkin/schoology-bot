# Open Questions

1. Does OpenClaw have a stable tool-calling interface we can map to our tool registry?
2. What are the supported memory backends and compaction triggers?
3. How does it handle clarifications (e.g., '4pl' -> '4pm')?
4. What is the recommended testing strategy for live APIs?
5. Do we need to adapt our Telegram bot, or does OpenClaw already have a Telegram IO?
6. How should we handle Schoology login state (Playwright) in an OpenClaw runtime?
