# OpenClaw Workspace (beta)

This workspace is mounted into the OpenClaw gateway container for beta evaluation.
Skills live under `openclaw_workspace/skills`.
Helper scripts live under `openclaw_workspace/tools`.
