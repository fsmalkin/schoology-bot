# OpenClaw Open Questions Archive

Status: closed by direction change on 2026-05-25.

The remaining OpenClaw questions are no longer active planning inputs because
OpenClaw is rollback/reference only.

## Superseded Questions
1. Can OpenClaw handle the current tool set end-to-end?
2. How does it handle clarifications for partial inputs?
3. Can it avoid tool-call loops without extra heuristics?
4. What is the best way to run offline tests and low-cost live simulation?
5. How should Schoology login state be handled in an OpenClaw runtime?

These concerns move to the Claude Managed Agents parity gate, tracked in
`docs/managed-agents/README.md`.
