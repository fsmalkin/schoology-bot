# Worklog

Execution state is tracked centrally on the FSM Engineering Board.
This file is the repo-local handoff ledger for cross-thread continuity.

## Entry Contract
Timestamp ET | Issue | Goal | Done | Next | Blockers | Validation run | Thread link

## Entries
2026-03-02 00:00 ET | initialization | Establish central tracking baseline | Initialized worklog and tracking templates | Use this format for each handoff | none | n/a | n/a
2026-03-05 23:33 ET | #19 | Workbench-first dashboard for assignments and tasks | Implemented two-experience dashboard with Workbench (Assignments, Tasks), Operations tab, new dashboard APIs/write guard, shared reminder display helpers, and docs/tests updates | Manual UAT in browser if desired; otherwise ready for normal use on `schoology-prod` | none | `node --test tests/dashboard_data.test.js tests/dashboard_workbench_data.test.js tests/dashboard_server.test.js tests/timezone_display.test.js`; `npm test`; `docker compose -p schoology-prod up -d --build`; `Invoke-WebRequest http://127.0.0.1:8787/api/meta` | n/a
2026-03-06 23:55 ET | #19 | Parent-first dashboard redesign with first-class assignment updates | Replaced the Workbench shell with `Home / All Schoolwork / Admin`, added `GET /api/home`, parent-facing assignment/follow-up read models, visible quick assignment actions, inline note/reminder composers, and updated dashboard docs/tests | Full browser UAT of the new flow on prod dashboard; otherwise ready for normal use after rebuild | none | `node --test tests/dashboard_workbench_data.test.js`; `node --test tests/dashboard_server.test.js` | n/a
2026-03-07 00:01 ET | #19 | Parent-first dashboard redesign with first-class assignment updates | Verified the rebuilt `schoology-prod` dashboard serves the new parent-first shell plus `GET /api/meta` and `GET /api/home`, and reran the full automated suite cleanly | Optional visual browser pass for final UX polish; implementation is otherwise complete | none | `npm test`; `curl.exe -s http://127.0.0.1:8787/`; `curl.exe -s http://127.0.0.1:8787/api/meta`; `curl.exe -s http://127.0.0.1:8787/api/home`; `docker compose -p schoology-prod logs --tail 80 dashboard` | n/a
2026-03-07 00:32 ET | #19 | Card-first dashboard redesign with review drawer | Replaced button-heavy card rails with click-to-open cards, converted the detail panel into a right-side slide-out drawer, rebuilt `All Schoolwork` as a card board with opt-in bulk mode, and added browser smoke coverage for the new interaction model | Rebuild `schoology-prod` and do a final visual pass on desktop/mobile; otherwise ready for normal use | none | `node --test tests/dashboard_server.test.js`; `node --test tests/dashboard_workbench_data.test.js`; `node --test tests/dashboard_ui_smoke.test.js` | n/a
2026-03-07 00:44 ET | #19 | Card-first dashboard redesign with review drawer | Rebuilt `schoology-prod`, verified the live dashboard serves the new card-first shell and right-side drawer, and visually checked both the closed board and open-drawer states in a browser screenshot | Optional copy/layout polish only; implementation and validation are complete | none | `npm test`; `docker compose -p schoology-prod up -d --build`; `curl.exe -s http://127.0.0.1:8787/`; `curl.exe -s http://127.0.0.1:8787/api/home`; Playwright live screenshots of closed board and open drawer | n/a
2026-03-08 00:27 ET | #19 | Minimal parent dashboard visual reset | Simplified the dashboard shell, reduced cards to a single scanning pattern, turned `Home` into one dominant tonight flow with quieter secondary sections, converted `All Schoolwork` into one grouped list, and collapsed the drawer to one editable section at a time | Optional browser-only polish pass; otherwise ready for normal use on `schoology-prod` | none | `node --test tests/dashboard_server.test.js`; `node --test tests/dashboard_workbench_data.test.js`; `node --test tests/dashboard_ui_smoke.test.js`; `npm test`; `docker compose up -d --build` (port collision on local duplicate stack); `docker compose -p schoology-prod up -d --build`; `curl.exe -s http://127.0.0.1:8787/`; `curl.exe -s http://127.0.0.1:8787/api/home`; `curl.exe -s http://127.0.0.1:8787/assets/dashboard.js` | n/a
2026-03-10 16:51 ET | #19 | Refresh Schoology button feedback on parent dashboard | Added visible busy/success feedback to the dashboard `Refresh Schoology` action so clicks disable the button, show `Refreshing...`, and report returned assignment counts; added browser smoke coverage for the busy-state and completion message; rebuilt `schoology-prod` and verified the live dashboard shows the in-progress and completion states on a real click | Ready for normal use; optional next step is deeper error-state UX if we want retry guidance for scrape failures | none | `node --test tests/dashboard_server.test.js tests/dashboard_ui_smoke.test.js`; `npm test`; `docker compose -p schoology-prod up -d --build`; `curl.exe -s http://127.0.0.1:8787/assets/dashboard.js`; Playwright live click check against `http://127.0.0.1:8787/` | n/a
2026-03-15 00:04 ET | n/a | Dashboard titles for no-link Schoology assignments | Added scraper/state/DB/dashboard title fallback for Schoology rows with visible text but no assignment link, plus reminder/task naming fallback and regression tests for the `Note: This material is not available within Schoology` row shape | Optional follow-up is backfilling old assignment-linked reminder titles/messages that were saved before this fix; current assignment cards and drawer titles are correct on `schoology-prod` | none | `node --test tests/text_utils.test.js tests/schoology.test.js tests/db.test.js tests/dashboard_workbench_data.test.js`; `npm test`; `docker compose -p schoology-prod up -d --build`; `docker compose -p schoology-prod logs --tail 80 dashboard`; live `GET /api/assignments?status=missing&includePending=true&includeIgnored=true`; live `GET /api/home` | n/a
2026-03-15 12:45 ET | n/a | Dashboard redesign — sidebar nav, metric row, 2-column layout | Rewrote `index.html`, `dashboard.css`, and `dashboard.js` to replace the hero/tab-bar shell with a dark sidebar, topbar breadcrumb, 4-card metric row, and a 2-column home grid (assignment sections left, overview panels right). Section labels updated for clarity. Fixed all test breakage from structural changes (server test text refs, smoke test section selectors, drawer z-index, mobile viewport nav). Rebuilt `schoology-prod` Docker image and restarted container. All 111 tests pass. | Ready for normal use | none | `npm test`; `docker build -t schoology-app:latest .`; `docker compose -p schoology-prod up -d --force-recreate --no-build dashboard`; verified `http://127.0.0.1:8787/` serves new design | n/a
2026-03-20 15:52 ET | n/a | Beta dashboard stabilization and core beta test suite | Added shared dashboard test fixtures, extended server coverage for `/beta` routes/assets, added dedicated beta browser smoke for assignment/task/reminder flows plus draft-preservation and partial-failure regressions, and fixed beta client-state issues around SVG click handling, request/rerender stability, and focus return after cards move buckets | Run full `npm test`, rebuild Docker runtime, and do a quick `/beta` desktop/mobile visual pass; if we treat this as reminder/task write-scope for release gating, run beta reset plus story suite/judge before human UAT | none | `node --test tests/time.test.js tests/dashboard_workbench_data.test.js tests/dashboard_server.test.js tests/dashboard_ui_smoke.test.js tests/dashboard_beta_ui_smoke.test.js` | n/a
2026-03-20 23:01 ET | n/a | Beta dashboard race and health-poll coverage completion | Added beta browser smoke coverage for stale delayed assignment-detail responses, close-before-response behavior, and timer-driven health-poll rerenders so the remaining `/beta` client-state gaps are now exercised automatically | Ready for the normal runtime rebuild/UAT cadence; automated dashboard and full-suite validation are green | none | `node --test tests/dashboard_beta_ui_smoke.test.js`; `node --test tests/dashboard_workbench_data.test.js tests/dashboard_server.test.js tests/dashboard_ui_smoke.test.js tests/dashboard_beta_ui_smoke.test.js`; `npm test` | n/a
2026-03-23 15:29 ET | #10,#14,#15,#16,#17,#20,#21,#22,#23 | Multi-issue Schoology correctness, memory, reminder, and UX wave | Shipped MUA/external-tool-link scrape fixes with detail fallback, stable assignment identity refresh, DB-backed chat memory + style preference, auto-cancel-on-resolve reminder cleanup, new `Will complete in class` status, MUA display-title expansion, richer follow-up/plain-language messaging, and beta runtime DB reset/restore hardening (`data/beta/agent.runtime.db`) | Optional manual beta/prod UX pass only; issue tracking can close the delivered items and keep #18 parked | none | `npm test`; `npm run beta:reset-memory`; `npm run stories:run`; `npm run stories:judge`; `docker compose -p schoology-prod up -d --build`; `curl.exe -s http://127.0.0.1:8787/api/health`; `curl.exe -s http://127.0.0.1:8788/api/health` | n/a
2026-03-30 20:50 ET | n/a | Make dashboard refresh feel trustworthy during slow Schoology scrapes | Confirmed live refreshes were succeeding but taking roughly 20-60s, then updated prod/beta dashboards to keep a persistent in-progress message, show elapsed time on the button, report completion duration, and raised dashboard container CPU/memory limits to better match the scrape workload | Rebuild/restart `schoology-prod`, run dashboard smoke coverage, and verify the live admin refresh flow shows clear progress/completion on prod | none | `node --test tests/dashboard_ui_smoke.test.js tests/dashboard_beta_ui_smoke.test.js`; `docker compose -p schoology-prod up -d --build`; Playwright live refresh against `http://127.0.0.1:8787/` | n/a
2026-05-25 23:37 ET | [#25](https://github.com/fsmalkin/schoology-bot/issues/25) | Pivot runtime plans from OpenClaw to Claude Managed Agents | Updated roadmap, backlog, system, architecture, dashboard, recovery, test-coverage, AGENTS overlay, and OpenClaw archive docs so Managed Agents is the top-priority dev-to-prod runtime path and OpenClaw is rollback/reference only; added `docs/managed-agents/README.md`; updated issue #25 title/body | Begin Managed Agents dev bridge implementation: session mapping, Telegram bridge, Claude custom tool loop, health/cost controls, and parity tests | GitHub Project board update blocked by local token missing `read:project`; repo issue updated | `git diff --check` | n/a
2026-05-25 23:53 ET | [#25](https://github.com/fsmalkin/schoology-bot/issues/25) | Build Managed Agents execution board with bidirectional repo links | Refreshed GitHub project auth, created labels, created sub-issues [#27](https://github.com/fsmalkin/schoology-bot/issues/27)-[#32](https://github.com/fsmalkin/schoology-bot/issues/32), linked them under #25, added all migration issues to the FSM Engineering Board, populated project metadata, and updated repo docs so planning docs link to issues/board and issue bodies link back to docs | Start #27 implementation: config/session mapping first; continue agent-side UAT until final pre-cutover UAT is ready for user | Need verify Anthropic/Claude Managed Agents credentials and dev Telegram target exist in env before live API/UAT | `gh project field-list`; `gh project item-list`; `gh issue view`; `git diff --check` pending after doc link patch | n/a
2026-05-26 00:09 ET | [#27](https://github.com/fsmalkin/schoology-bot/issues/27) | Managed Agents session mapping and dev config | Added Managed Agents config/validation, `managed_agent_sessions` SQLite mapping, migration coverage, and docs; closed #27 and moved #28 active on the FSM Engineering Board | Continue #28 bridge/UAT implementation | Live Claude UAT needs `ANTHROPIC_API_KEY`, `CLAUDE_MANAGED_AGENT_ID`, `CLAUDE_MANAGED_ENVIRONMENT_ID`, and a safe dev Telegram target | `node -c src\config.js`; `node -c src\db.js`; `node --test tests/managed_agent_sessions.test.js tests/migrations.test.js`; `npm test` | n/a
2026-05-26 00:09 ET | [#28](https://github.com/fsmalkin/schoology-bot/issues/28) | Managed Agents Telegram dev bridge | Added runtime router, Managed Agents REST/SSE client, Telegram-to-session bridge, assistant text collection, local custom Schoology tool result handling, bridge tests, and bidirectional repo doc updates | Harden #29 custom tool loop and run live dev UAT once Claude credentials/environment are present | Live Claude/Telegram UAT blocked on Managed Agents credentials/environment and safe dev Telegram target | `node --test tests/managed_agent_bridge.test.js tests/managed_agent_sessions.test.js tests/migrations.test.js`; full suite pending | n/a
2026-05-26 00:16 ET | [#29](https://github.com/fsmalkin/schoology-bot/issues/29) | Managed Agents custom tool loop through Schoology tools | Added exported Managed Agents custom tool definitions, unsupported custom-tool error handling, built-in tool confirmation denial, deterministic invalid-arg errors, bounded tool-result payloads, loop guardrails, and mock UAT coverage | Continue live-ready hardening and parity story integration once credentials are available | Live Claude/Telegram UAT blocked on Managed Agents credentials/environment and safe dev Telegram target | `node -c src\managed_agent_tools.js`; `node --test tests\managed_agent_bridge.test.js tests\managed_agent_sessions.test.js tests\managed_agent_tools.test.js` | n/a
2026-05-26 00:16 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Managed Agents parity story suite and judge gate | Updated the story suite runner to call `runChatMessage`, preserving legacy OpenAI behavior by default while enabling Managed Agents parity runs via `RUNTIME_STACK=managed-agents` or `MANAGED_AGENTS_ENABLED=1` | Run Managed Agents story suite once Claude credentials/environment and safe dev Telegram target are available | Live Claude/Telegram UAT blocked on Managed Agents credentials/environment and safe dev Telegram target | `node -c scripts\run_agentic_story_suite.mjs`; `node --test tests\managed_agent_bridge.test.js tests\managed_agent_tools.test.js` | n/a
2026-05-26 22:07 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Managed Agents API-managed resources and live dev smoke | Created/reused dev/prod Claude Managed Agents environments and agents via API, wrote IDs into ignored env files, adapted custom tool schemas for Claude API constraints, fixed SSE cleanup, and completed live dev no-tool plus custom-tool smoke sessions against isolated artifact DBs | Create dev Telegram bot/chat id, run Telegram UAT, then run Managed Agents story suite/judge | Telegram dev/prod bot tokens and chat IDs are still missing | `node --test tests\managed_agent_tools.test.js tests\managed_agent_bridge.test.js`; live dev smoke sessions `sesn_01MpMQ7zsv6VvTvipDysJJQg`, `sesn_01WrkyksiBAUXQjgMaeMY8QJ`; `npm test`; `git diff --check` | n/a
2026-05-26 22:40 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Managed Agents dev parity and beta-thread UAT readiness | Updated Managed Agents reminder-default prompt policy and dev cloud agent to version 2, added deterministic story-clock plumbing through `runChatMessage`/tool execution, added prompt-policy and date-clock tests, passed legacy and Managed Agents story gates/judges, rebuilt Docker prod services, verified dashboard health/logs/browser load, and sent a beta-thread outbound Telegram smoke message | Hand off for human inbound Telegram UAT in the beta thread, then continue #31 health/event log/idle controls before prod canary | Full inbound Telegram E2E still requires a user-sent beta-thread message; Codex Telegram route for repo is not provisioned | `node --test --test-concurrency=1 tests/tool_runner_recurrence_assumptions.test.js tests/managed_agent_definitions.test.js tests/managed_agent_bridge.test.js`; `npm test`; `npm run stories:gate`; `node scripts/with_env.js .env.managed-dev node -r dotenv/config scripts/run_agentic_story_suite.mjs`; `npm run stories:judge`; `git diff --check`; `docker compose -p schoology-prod up -d --build`; `curl.exe -s http://127.0.0.1:8787/api/health`; Docker service log tails; Playwright live dashboard smoke; Telegram outbound message id 196 | n/a
2026-05-26 23:42 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Switch beta Telegram UAT from OpenClaw to Managed Agents | Stopped the legacy `schoology-openclaw-beta` stack, confirmed Fred's inbound beta message created/updated a `managed_agent_sessions` row for `schoology-dev` and the Claude dev environment, then started `schoology-managed-dev-telegram-agent` as the active beta bot poller using beta data paths plus Managed Agents config | Continue Fred UAT against the beta Telegram thread; when UAT is done, stop `schoology-managed-dev-telegram-agent` or fold it into compose for the next slice | none | `docker compose -f docker-compose.beta-openclaw.yml -p schoology-openclaw-beta down`; `docker compose -f docker-compose.beta-openclaw.yml --profile dashboard -p schoology-openclaw-beta down`; managed session DB query; `docker run -d --name schoology-managed-dev-telegram-agent ...`; `docker logs --tail 80 schoology-managed-dev-telegram-agent`; `docker compose -p schoology-prod ps` | n/a
2026-05-26 23:59 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Subagent-driven Managed Agents UAT gap closure | Spawned four subagents for live JTBD UAT, Telegram/runtime ownership, docs/gap review, and deterministic regressions; confirmed deterministic selected tests passed (53/53), old beta is stopped, prod is healthy, and a broader isolated live Managed Agents JTBD UAT passed with assignment listing, status update, note, assignment reminder, recurring task, correction, monthly fallback, daily summary, and due-reminder drain | Capture one more beta-thread inbound message against the current Dockerized `schoology-managed-dev-telegram-agent`, then decide whether #31 idle/session health is a post-UAT gate or a UAT blocker | Current Dockerized managed-dev container has startup evidence but no inbound message since it replaced the foreground poller; live scrape/refresh and login-failure alerting remain deterministic-test covered only in this slice | Subagent A live JTBD UAT (`sesn_01HpAfEZH9345jZQC5Fh9dbB`); Subagent D selected deterministic test suite 53 pass/0 fail; Docker/log/session checks; docs gap review | n/a
2026-05-27 00:00 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Cleanup checkpoint after Managed Agents UAT | Removed the only untracked cleanup candidate, an empty `openclaw_workspace/.pi/` directory, verified the repo has no tracked or untracked worktree changes, and created a checkpoint for the cleaned state | Capture one more beta-thread inbound message against the current Dockerized `schoology-managed-dev-telegram-agent` | Current Dockerized managed-dev container still needs a fresh inbound beta-thread message to prove this exact instance receives and replies | `git status --short --branch`; `git clean -nd`; `git commit --allow-empty -m "chore: checkpoint cleaned managed uat state"` | n/a
2026-05-27 00:30 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Refresh stale beta Schoology auth and rerun managed-dev repro | Confirmed beta storage was stale, refreshed it through interactive Chrome login, verified the Dockerized managed-dev container lands on Schoology grades without login controls, ran live scrape successfully with 0 missing assignments, fixed duplicate Managed Agents action-id execution observed during the first repro, rebuilt prod services, recreated `schoology-managed-dev-telegram-agent`, and reran the managed-dev repro successfully with session `sesn_018Gc6shAGPFyDd6DGXhLSw9` | Capture one fresh beta-thread inbound message against the current Dockerized managed-dev poller, then hand off for Fred UAT | Telegram container inbound proof remains outstanding; repo is still ahead of origin and behind one remote commit | Storage probe; `docker exec schoology-managed-dev-telegram-agent node src/run.js scrape`; live managed-dev repro; `node --test --test-concurrency=1 tests\managed_agent_bridge.test.js`; `npm test`; `docker compose -p schoology-prod up -d --build`; `curl.exe -s http://127.0.0.1:8787/api/health`; `docker logs --tail 80 schoology-managed-dev-telegram-agent` | n/a
2026-05-27 00:37 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Reconcile remote agent shell decision gate before push | Merged the remote docs-only `docs: add agent shell/runtime decision gate` commit, resolved `docs/BACKLOG.md` and `docs/ROADMAP.md` so the May 17 gate is recorded as completed with Claude Managed Agents selected, and kept the Managed Agents dev-to-prod path as current P1 | Push reconciled `main`, then capture one fresh beta-thread inbound message against the current Dockerized managed-dev poller | Telegram container inbound proof remains outstanding | `git diff --cached --check`; `node --test --test-concurrency=1 tests\managed_agent_bridge.test.js tests\managed_agent_sessions.test.js tests\managed_agent_definitions.test.js` | n/a
2026-05-27 00:50 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Fix Telegram Markdown table formatting | Added Telegram renderer conversion from Markdown pipe tables to compact lists, updated legacy and Managed Agents prompts to avoid Markdown tables, updated the dev cloud agent to version 3, rebuilt prod services, recreated the managed-dev Telegram poller, and sent a live beta-thread formatting smoke (message id 208) | Capture one fresh inbound beta-thread message against the current Dockerized managed-dev poller | Telegram container inbound proof remains outstanding | `node -c src\telegram_format.js`; `node -c src\agent.js`; `node -c src\managed_agent_definitions.js`; `node --test --test-concurrency=1 tests\telegram_format.test.js tests\managed_agent_definitions.test.js`; `npm test` (154/154 passing); `managed_agents_admin.mjs update-agent ... version 2 dev`; `docker compose -p schoology-prod up -d --build`; live beta-thread formatting smoke id 208 | n/a
2026-05-27 01:08 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Close submitted/ungraded icon-query UAT gap | Validated that Schoology's hidden grade-pending/dropbox icon text is already scraped as `Submitted, awaiting grade`, found the Managed Agents UAT gap was the conversational query path, added first-class `submitted_awaiting_grade` filtering to `list_assignments`, updated legacy/Managed prompts and tool schema, updated the dev cloud agent to version 4, rebuilt prod Docker services, recreated `schoology-managed-dev-telegram-agent`, live-smoked the managed query with session `sesn_017YiTWYQ4FT6unZ6DLXaMp8` returning the two historical beta rows, committed `759ff98`, and pushed `main` | Run a fresh beta-thread inbound UAT prompt against the recreated Dockerized managed-dev poller | Telegram inbound proof for this exact recreated container remains outstanding | `node --test --test-concurrency=1 tests/db.test.js tests/managed_agent_definitions.test.js tests/managed_agent_tools.test.js tests/schoology.test.js`; `npm test` (155/155 passing); `git diff --check`; live local DB query for `status=submitted_awaiting_grade`; `managed_agents_admin.mjs update-agent ... version 3 dev`; `docker compose -p schoology-prod up -d --build`; `docker run ... schoology-managed-dev-telegram-agent`; `curl.exe -s http://127.0.0.1:8787/api/health`; live Managed Agents smoke session `sesn_017YiTWYQ4FT6unZ6DLXaMp8`; `git push origin main` | n/a
2026-05-27 01:32 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Thorough managed UAT after submitted/ungraded miss | Added required story-gate `S9` for submitted-but-ungraded icon queries, expanded judge evidence for assignment list outputs, ran two independent subagent audits, found and fixed a second Managed Agents bridge issue where speculative pre-tool assistant text could be concatenated with confirmed post-tool text, rebuilt prod Docker services, recreated `schoology-managed-dev-telegram-agent`, and verified the recreated container can call Managed Agents against beta data and return the two submitted/ungraded rows | Commit/push this UAT checkpoint, then ask Fred to send a fresh beta-thread inbound UAT prompt to prove real Telegram receive -> managed agent -> formatted reply on the current container | True Telegram inbound proof still needs a human-sent message; automated tests and Docker exec can prove everything except Telegram delivering a user update to the poller | `node -c scripts\run_agentic_story_suite.mjs`; `node -c scripts\judge_agentic_story_suite.mjs`; `node --test --test-concurrency=1 tests/managed_agent_bridge.test.js`; targeted submitted/ungraded tests (14/14); `npm test` (156/156 passing); legacy story gate + judge `20260527-051545`; Managed Agents story gate failed once on S1, then passed after bridge fix with artifact `20260527-052502`; live beta-data Managed Agents smokes `sesn_01LaHRC29pzSYAsH6uTvcbvy` and Docker exec `sesn_01BdpvdhXf1VwMAhqEWxEoSg`; `docker compose -p schoology-prod up -d --build`; recreated managed-dev poller; dashboard health/log checks | n/a
2026-05-28 01:01 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Harden BCPS auth refresh and diagnose saved-credential failure | Updated the Microsoft/BCPS auth flow to prefer keep-signed-in, start Schoology SAML directly for remote-auth pages, use the visible BCPS local credential form before AzureAD claim-provider routing, and preserve explicit saved-credential rejection diagnostics with body text snippets; rebuilt prod services and recreated `schoology-managed-dev-telegram-agent`; live beta scrape now reaches the rendered BCPS login form but BCPS rejects the saved env credentials | Refresh `.env`/`.env.managed-dev` `SCHOLOGY_USERNAME`/`SCHOLOGY_PASSWORD` or complete interactive login, then rerun `docker exec -e DEBUG_DUMP=true schoology-managed-dev-telegram-agent node src/run.js scrape` and a beta-thread refresh prompt | Automated refresh is blocked by BCPS returning "The username or password provided in the request are invalid" for the saved credentials; beta storage remains at 2026-05-27 00:44 ET | `node -c src/schoology.js`; `node --test --test-concurrency=1 tests/schoology_login.test.js tests/schoology.test.js`; `npm test` (160/160 passing); `docker compose -p schoology-prod up -d --build`; recreated managed-dev poller; live beta scrape diagnostic `data/beta/login-diagnostic.json` at 2026-05-28T04:59:11.731Z | n/a
2026-05-28 09:07 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Fix BCPS Account provider path and refresh beta auth | Verified `.env`, `.env.managed-dev`, `.env.beta`, and the managed-dev container all use the same Schoology credential fingerprints; reproduced that `Login with your BCPS Account` advances through Microsoft password/KMSI while the local myBCPS fields reject; updated auth to prefer the BCPS Account provider, wait for Microsoft credential controls after provider selection, avoid submitting local B2C fields while the provider button is present, rebuilt services, recreated `schoology-managed-dev-telegram-agent`, and refreshed beta successfully | Run beta-thread Telegram refresh/query UAT on the current poller | none for auth refresh; Telegram inbound proof still needs a beta-thread prompt | `node -c src/schoology.js`; `node --test --test-concurrency=1 tests/schoology_login.test.js tests/schoology.test.js`; `docker compose -p schoology-prod up -d --build`; recreated managed-dev poller; `docker exec -e DEBUG_DUMP=true schoology-managed-dev-telegram-agent node src/run.js scrape` -> 26 missing assignments, beta storage/state refreshed at 2026-05-28 09:07 ET; `npm test` (160/160 passing) | n/a
2026-05-28 10:04 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Add managed-agent web search with kid-safe guardrails | Enabled only `web_search`/`web_fetch` from the Claude Managed Agents built-in toolset, kept shell/file tools disabled, added kid-safe input and output filtering in legacy and Managed Agents runtime paths, updated docs, updated the dev cloud agent to version `5`, rebuilt prod services, recreated `schoology-managed-dev-telegram-agent`, and validated that Schoology queries still use deterministic local tools | Hand off for beta-thread UAT of a safe web lookup and ordinary Schoology query on the current poller | No blocker; true inbound Telegram proof still needs a human-sent beta-thread prompt | `node -c src/kid_safe_content_filter.js src/managed_agent_definitions.js src/managed_agent_bridge.js src/agent.js`; targeted managed/kid-safety tests (17/17); `npm test` (167/167 passing); `git diff --check`; `npm run managed:render-agent:dev`; `managed_agents_admin.mjs update-agent ... version 4 dev` -> version `5`; live safe web smoke `sesn_01XemzqCm6qiasnsqKYScfvA`; unsafe prompt blocked with no session; live/container submitted-ungraded smokes `sesn_01KgDVpc4b4N7UzPnWZ73asL`, `sesn_01Uw4f9QtyPcvkTeKJxdPFCW`; `docker compose -p schoology-prod up -d --build`; dashboard health; Dockerized safety smoke | n/a
2026-05-28 16:54 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Fix stale beta managed session after web-search rollout | Diagnosed beta reply "I have no web search capability" as reuse of `sesn_01U5ZSP3XDA5pz3xyUfJVqrH`, created before dev agent version `5`; added managed-agent definition revision metadata and automatic fresh-session creation when a chat session revision is stale; reset the beta thread session and verified the recreated Dockerized poller now creates `sesn_01VJVaDoeTseHTjoPLdvHobb` with the new revision and answers that web search is available | Ask Fred to retry the beta-thread web-search prompt; commit/push this revision-guard checkpoint | none | `node -c src/managed_agent_bridge.js src/managed_agent_definitions.js`; targeted managed tests (18/18); `npm test` (168/168 passing); `git diff --check`; beta DB reset via `resetManagedAgentSession`; `docker compose -p schoology-prod up -d --build`; recreated `schoology-managed-dev-telegram-agent`; Docker exec live beta-chat smoke -> "Yes, I can search the web..." | n/a
2026-05-28 09:40 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Add forward guardrail for BCPS auth regression | Tightened the BCPS auth regression test so it proves the provider button is clicked, local myBCPS fields are not filled, Microsoft email/password fields receive the saved credentials, and both Microsoft submits occur | Commit/push guardrail, then continue beta-thread Telegram UAT on the current poller | none | `node -c tests/schoology_login.test.js`; `node --test --test-concurrency=1 tests/schoology_login.test.js`; `node --test --test-concurrency=1 tests/schoology_login.test.js tests/schoology.test.js` (10/10 passing) | n/a
2026-05-28 17:16 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Add Claude Managed Agent memory store | Added Managed Agents memory-store config, session resource attachment, memory file tool allow-listing, memory guardrails, admin memory-store commands, tests, and docs; created dev memory store `memstore_01F4pmYqg2GRep72inSfK2zi`, seeded memory policy, wrote `/preferences/parent_preferences.md`, updated the dev cloud agent to version `7`, rebuilt Docker services, recreated `schoology-managed-dev-telegram-agent`, and verified the recreated poller recalls Fred's concise beta UAT preference from managed memory | Ask Fred to try a beta-thread memory prompt, then continue #31 health/event-log/idle controls before prod canary | none | `node -c src/config.js src/managed_agent_client.js src/managed_agent_bridge.js src/managed_agent_definitions.js scripts/managed_agents_admin.mjs`; targeted managed tests (23/23, then 17/17 after revision cleanup); `npm test` (170/170 passing); `git diff --check`; `managed_agents_admin.mjs create-memory-store`; `managed_agents_admin.mjs create-memory`; live write session `sesn_01Xfm4iyQmigceMYcF9kDhBg`; fresh recall session `sesn_015nNuouPRitk1mvHgPWLF7e`; `managed_agents_admin.mjs update-agent ... version 6 dev` -> version `7`; `docker compose -p schoology-prod up -d --build`; recreated managed-dev poller; Docker exec recall `sesn_015dSmmxjCgtVVTei98gVkDF`; dashboard health | n/a
2026-05-28 17:41 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Fix date-filtered bulk status timeout | Diagnosed Fred's beta-thread "mark everything before 4/4 as no action needed" failure as a Managed Agents stream timeout caused by model-enumerated bulk status work; added deterministic `bulk_update_assignments_by_filter`, mapped "no action needed" to the ignored local status, added school-year shorthand-date correction, updated legacy/Managed prompts and schemas, updated the dev cloud agent to version `8`, rebuilt Docker, recreated `schoology-managed-dev-telegram-agent`, applied the original beta update to the 7 intended rows, and reset the beta chat managed session | Ask Fred to retry ordinary beta-thread UAT; continue #31 health/event-log/idle controls before prod canary | none | targeted filtered-bulk/Managed tests (28/28); `npm test` (174/174 passing); `git diff --check`; live copied-DB repro `sesn_01Bj1VcoqpqDHWerrf8iTs88` updated 7 rows in one tool call; beta DB snapshot `artifacts/manual-snapshots/agent.runtime.before-filtered-bulk-20260528-173906.db`; real beta update active missing 16 -> 9 and active before 2026-04-04 count 0; container retry `sesn_01Rc94cuxVMf1pJZ1gWAow66` completed in one stream pass with 0 remaining matches; `docker compose -p schoology-prod up -d --build`; recreated managed-dev poller | n/a
2026-05-28 18:02 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Preserve managed retry context across session resets | Diagnosed the beta "try again" amnesia as a local session reset without transcript context: Claude managed memory persisted durable preferences, but the bridge did not save the failed Telegram request before throwing. Added bounded last-failed-request metadata in `managed_agent_sessions`, carryover during forced session recreation, retry shorthand expansion for `try again`, and a duplicate-write caution in retry context; updated Managed Agents docs to distinguish Claude managed memory from local retry context | Hand off for beta-thread UAT of ordinary memory/retry behavior; continue #31 health, event-log, and idle controls before prod canary | True inbound Telegram proof still needs a human-sent beta-thread prompt; direct container live smoke covers the bridge/Claude path but not Telegram delivery from a human update | `node --test --test-concurrency=1 tests/managed_agent_bridge.test.js tests/managed_agent_sessions.test.js` (23/23); `npm test` (175/175 passing); `git diff --check`; `docker compose -p schoology-prod up -d --build`; recreated `schoology-managed-dev-telegram-agent`; dashboard health OK; Docker exec live retry smoke session `sesn_012qVBQKzbosWvGy85XTWgRR` replied `retry context smoke OK` and cleared `lastFailedUserText` | n/a
2026-05-28 22:58 ET | [#33](https://github.com/fsmalkin/schoology-bot/issues/33) | Stop ignored assignments leaking through bucketed missing lists | Pulled in the latest filed bug, reproduced the mismatch in code: flat `listAssignments` honored `includeIgnored=false`, but bucketed output was built from unfiltered rows, so agent-visible missing-list buckets could expose already ignored/no-action rows. Changed bucketed output to bucket only filtered rows, kept `unfilteredTotal` for diagnostics, and added regression coverage for ignored and pending filters | Commit/push the checkpoint and close #33 | none | targeted DB/summary/Managed tests passed (29/29); `npm test` (176/176); `git diff --check`; `docker compose -p schoology-prod up -d --build`; recreated `schoology-managed-dev-telegram-agent`; dashboard health OK; live beta DB probe showed default bucketed missing list has 9 visible rows, 0 ignored bucket rows, and no issue-example title leaks; live Managed Agents smoke `sesn_01XDiiNc86qsAVQKV8WS8kHn` called `list_assignments` and reply did not include issue-example titles; cleaned smoke session row | n/a
2026-05-28 23:04 ET | [#34](https://github.com/fsmalkin/schoology-bot/issues/34) | Plan OpenClaw code/repo removal | Created #34 for the new direction to tear out OpenClaw instead of keeping it as rollback/reference, and updated AGENTS, README, Roadmap, Backlog, System, Architecture, and Managed Agents migration docs so current prod Docker is the rollback target and OpenClaw artifacts are removal candidates before prod canary | Execute #34 cleanup: remove OpenClaw source, compose, scripts, tests, and active docs; then run Docker/managed-dev validation | none | duplicate check: `OpenClaw removal tear out cleanup Managed Agents` returned no existing issue; `git diff --check` | n/a
2026-05-28 23:25 ET | [#34](https://github.com/fsmalkin/schoology-bot/issues/34) | Remove OpenClaw runnable repo surface | Deleted OpenClaw source, compose, Dockerfile, submodule, workspace, reset/native install/cron scripts, active archive docs, and package scripts; simplified dashboard service/command logic to prod Docker only; simplified ops scripts around Docker rollback and Managed Agents beta data; renamed cron tool coverage away from OpenClaw; updated active docs so OpenClaw is not a runnable or rollback path | Rebuild prod Docker, recreate managed-dev poller, run Managed Agents story/UAT validation, then continue #31 health/event-log/idle controls | none | `rg -n "openclaw|OpenClaw|schoology-tool-api|beta-openclaw|openclaw_workspace|beta:reset-memory|RUNTIME_STACK=openclaw"` returns only historical `WORKLOG`/`COMPLETED` entries; `node -c src\dashboard_data.js`; ops dry-runs for start/backup/register; targeted dashboard/runtime and Managed Agents tests passed; `npm test` (175/175); `git diff --check` clean except line-ending warnings | n/a
2026-05-28 23:43 ET | [#34](https://github.com/fsmalkin/schoology-bot/issues/34) | Close subagent cleanup and restore-safety gaps | Patched stale active references in `.env.beta.example`, `PLANS.md`, and dashboard audit; added restore preflight before stopping prod; restored beta bind-mounted SQLite only through container-side install; relaxed restore-drill requirements to the canonical Docker prod DB snapshot; switched backup export to SQLite online backup instead of raw live-volume `cp` | Commit/push the follow-up checkpoint, update #34/#30/#25 handoffs, then continue #31 health/event-log/idle controls | True inbound Telegram proof still requires a human beta-thread message; automated and direct container smokes are otherwise green | subagent static/ops/UAT audits; repo-wide retired-runtime scan returns only historical `WORKLOG`/`COMPLETED`; backup/restore/restore-drill dry-runs; Docker online backup integrity smoke (`integrity_check=ok`); `git diff --check` clean except line-ending warnings | n/a
2026-05-29 00:28 ET | [#31](https://github.com/fsmalkin/schoology-bot/issues/31) | Managed Agents health, event log, and idle cost controls | Added sanitized `managed_agent_events`, Managed Agents bridge heartbeat/event writes, value-level secret redaction, proactive idle sweep from the managed Telegram poller, dashboard/admin Managed Agents status sourced from the active managed-dev beta DB, repeated tool-error/cost-risk alerts, and Admin UI coverage; rebuilt prod Docker and recreated `schoology-managed-dev-telegram-agent`; live managed-dev smoke passed for scrape, submitted-awaiting-grade, safe web search, kid-safety block, event log, heartbeat, and idle sweep | Commit/push #31 checkpoint, update #31/#25 handoffs, then decide whether #32 prod canary is ready after human beta-thread UAT | True inbound Telegram proof still benefits from a fresh human beta-thread prompt against the current recreated poller; remote Claude session termination remains local-reset-only until the API documents explicit termination | subagent audits (privacy, dashboard, acceptance) resolved; `node -c` touched runtime files; targeted #31 tests; `npm test` (187/187); `git diff --check`; `docker compose -p schoology-prod up -d --build`; recreated managed-dev poller; dashboard health all services OK; live scrape -> 26 missing; live Managed Agents smokes for submitted/ungraded, safe web, unsafe block; event/heartbeat DB probe; legacy story judge `20260529-042231` pass; Managed Agents story judge `20260529-042521` pass | n/a
2026-05-29 19:37 ET | [#30](https://github.com/fsmalkin/schoology-bot/issues/30) | Verify true inbound Telegram on current Managed Agents beta poller | Fred sent real beta-thread messages to `schoology_beta_bot`; the current `schoology-managed-dev-telegram-agent` received two batches, replied to both, created/reused the `schoology-dev` Claude Managed Agents session, executed `list_assignments` on the follow-up, and wrote `session_created`, `session_reused`, `custom_tool_result`, and `turn_completed` events with heartbeat `status=running` | Hand off for Fred's broader UAT on the existing beta thread; after UAT sign-off, prepare #32 prod canary with the current committed Docker prod runtime as rollback | none for #30; #32 still needs human UAT sign-off and secrets-hygiene decision | `docker logs --since 2026-05-29T23:32:00Z --timestamps schoology-managed-dev-telegram-agent`; `managed_agent_sessions` DB query for beta thread; `managed_agent_events` DB query for beta thread; heartbeat read from `data/beta/health/managed-agent-bridge.heartbeat.json`; `git status --short --branch` showed `main` synced with `origin/main` at `612cda3` | n/a
2026-05-30 01:26 ET | [#32](https://github.com/fsmalkin/schoology-bot/issues/32) | Prepare vault-backed Managed Agents prod cutover | Added `*_FILE` secret loading, Windows Credential Manager import/verify/export/sanitize tooling, ignored Docker secret/runtime-env export flow, managed-prod compose override for scheduler/Telegram/dashboard, local tracked-file secret scan, docs, and regression fixes from story validation for model-supplied reminder times plus bucketed submitted-awaiting-grade story checks; created a Docker/data backup snapshot and rebuilt the current standard prod stack healthy without the managed-prod override | Rotate/import required prod secrets into Windows Credential Manager, export vault-backed prod secret files, sanitize ignored env files, then start `docker-compose.managed-prod.yml` and run prod Telegram/dashboard UAT | Cutover intentionally blocked until rotated prod credentials exist in Windows Credential Manager; `create_schoology_pre_cutover_snapshot.ps1` was not run because it copies `.env` before sanitization | `node -c` touched config/admin/story/tool files; vault script dry-runs; targeted config/Managed/tool/dashboard tests; `npm run secrets:scan`; `npm run stories:gate`; Managed Agents story suite plus judge (`artifacts/agentic-story-suite/20260530-051829`); `npm test` (192/192); `git diff --check`; `scripts/backup_schoology_state.ps1 -RuntimeMode docker -SkipSyncCopy` -> `D:\backups\schoology\local\snapshots\20260530-012336`; `docker compose -p schoology-prod up -d --build`; dashboard/scheduler/telegram health OK | n/a
2026-05-30 01:48 ET | [#32](https://github.com/fsmalkin/schoology-bot/issues/32) | Cut production over to Managed Agents | Updated the secrets plan to import current live values without provider-side rotation, imported required prod secrets into Windows Credential Manager, exported Docker secret files and `data/runtime/prod.env`, sanitized ignored `.env*` files with rollback backups, updated prod Claude agent to version 2, created prod memory store `memstore_01QsPa8eh4MkvTntgJxhhQEY`, started scheduler/Telegram/dashboard with `docker-compose.managed-prod.yml`, ran prod Managed Agents UAT, sent outbound prod Telegram smoke message `645`, removed UAT note/reminder artifacts, and patched dashboard quick commands for managed-prod rollback/current commands | Monitor 24h stabilization and capture one human-sent prod Telegram inbound message against the managed-prod poller | Human inbound Telegram proof still needs a user reply in the prod chat; ACL tightening for local secret/export backup files is best-effort on this machine due missing `SeSecurityPrivilege` | `powershell scripts/manage_schoology_secrets.ps1 -Action import/verify/export/sanitize-env -Environment prod`; prod Claude `update-agent` -> version 2; prod memory store create; `docker compose -f docker-compose.yml -f docker-compose.managed-prod.yml -p schoology-prod up -d --build`; prod UAT session `sesn_01Qt7GN723HL8eDgNQE1qMZE` covered help, refresh, missing, submitted-awaiting-grade, scored list, safe web, kid-safety input block, note, reminder, daily summary, and bulk no-op; UAT cleanup confirmed 0 marker notes/tasks/active sessions; dashboard health OK with `schoology-prod`, active sessions 0, no alerts; `node --test tests/dashboard_data.test.js`; `npm test` (187 pass, 5 skip); `npm run secrets:scan`; `git diff --check` | n/a
2026-05-30 08:36 ET | [#32](https://github.com/fsmalkin/schoology-bot/issues/32) | Live read-only Managed Agents prod UAT | Ran a separate non-mutating prod Managed Agents UAT pass after the controlled-write smoke, using synthetic chat `prod-uat-readonly-1780144457282`; verified help, missing list, submitted-awaiting-grade, scored list, daily summary, safe web lookup, kid-safety input block, and a date-filtered bulk dry-run; reset the synthetic session locally afterward | Capture one human-sent prod Telegram inbound message and continue 24h stabilization monitoring | No Schoology Bot data writes observed; telemetry/session/event rows were expected runtime evidence | Session `sesn_011z8MnT76WYrv7VmtAyiUZs`; before/after counts unchanged (`assignment_notes` 30, `tasks` 18, manual statuses 18); no dangerous write tools executed; bulk dry-run returned `dryRun=true`, `updatedCount=0`; all prompts OK | n/a
2026-06-01 08:27 ET | [#35](https://github.com/fsmalkin/schoology-bot/issues/35) | Fix future-due assignment overdue classification | Added server-side `dueCategory` fields, routed future-due assignments to `Soon` in daily summaries and `Coming up` in dashboard schoolwork, changed dashboard pills to use backend local-date classification, updated Managed Agents prod agent to version 3, rebuilt the managed-prod Docker stack | Monitor the next daily summary and ordinary Telegram UAT | none | `npm test` (192 pass, 5 skip); `node --test --test-concurrency=1 tests/time.test.js tests/summary_db.test.js tests/readable_messages.test.js tests/dashboard_workbench_data.test.js tests/managed_agent_definitions.test.js tests/managed_agent_tools.test.js tests/tool_runner_cron_tools.test.js`; `node --test --test-concurrency=1 tests/dashboard_due_category_ui.test.js`; drafting checker OK; `git diff --check`; `docker compose -f docker-compose.yml -f docker-compose.managed-prod.yml -p schoology-prod up -d --build`; dashboard health OK | n/a
